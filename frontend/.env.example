# Copy to .env for local dev, or set VITE_API_URL in your Vercel
# project settings for production.
#
# Local dev (Vite proxies /api -> http://localhost:8000):
VITE_API_URL=/api/v1
#
# Production example (Render backend URL):
# VITE_API_URL=https://hiring-platform-api.onrender.com/api/v1
